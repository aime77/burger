<li class="list-group-item">
    {{burger_name}}

    <button class="change-devoured" data-id="{{id}}" data-devour="{{devoured}}">
        {{#if devoured}}Eat Me!{{else}}Yum Yum!{{/if}}
    </button>
</li>